/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', "plus.unsplash.com"], // Add the domain(s) you want to allow
      },
};

export default nextConfig;
